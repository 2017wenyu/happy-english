# 2026-03-27 工作日志

## profile 页面清理签到功能


## index 页面修复

- 修复 `progress` 组件 `percent` 表达式运算问题：在 `index.js` 的 `data` 里新增 `totalMasteredPct` 字段并在 `loadStats` 里赋值，WXML 改用 `{{totalMasteredPct}}`
- 修复"全部220词混合闪卡"橙色按钮底部与总进度卡片之间无间距问题：给 `.mode-section` 加 `margin-bottom: 24rpx`

## profile 页面渲染错误修复 + 样式优化

- 修复 `Expected updated data but get first rendering data` 渲染层错误：`progress` 组件 `percent` 属性不支持表达式 `{{totalMastered / totalWords * 100}}`，改为使用 JS 预计算的 `{{totalMasteredPct}}`
- 家长面板按钮（`parent-btn`）和弹窗按钮（`modal-btn`）补充 `text-align: center` 确保居中
- `parent-btns` 补充 `margin-top: 8rpx` 与标题间距统一

